import React from 'react'

const CardOldVersion = () => {
    return (
        <>
            {/* <div className={styles.card} key={index}>
                            <div className={styles.containImg}>
                                <img className={styles.img} src={image.img} alt="" />
                            </div>
                            <div className={styles.details}>
                                <h1 className={styles.name}>{image.name}</h1>
                                <div className={styles.priceSection}>
                                    <div className={styles.priceLabel}>
                                        <span className={styles.price}>{image.price}</span>
                                    </div>
                                    <div className={styles.rating}>{image.rating}</div>
                                </div>
                                <div className={styles.offer}>{image.offers}</div>
                                <div className={styles.btnContainer}>
                                    <button className={`${styles.addToCartBtn} ${styles.btn}`}>Add To Cart</button>
                                    <button className={`${styles.buyNowBtn} ${styles.btn}`}>Buy Now</button>
                                </div>
                            </div>
                        </div> */}
        </>
    )
}

export default CardOldVersion;